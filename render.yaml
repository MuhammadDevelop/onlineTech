services:
  - type: web
    name: fastapi-backend
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    region: frankfurt
    runtime: python  # Bu yerda Python ko'rsatiladi

